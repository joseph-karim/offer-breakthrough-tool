import React from 'react';

// This is an empty component that replaces the "Get AI Help" button
// It doesn't render anything since we now have the persistent chat
export const EmptyAIButton: React.FC = () => {
  return null;
};
